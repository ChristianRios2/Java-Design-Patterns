package design_patterns.strategy;
import java.util.*;
public class BinarySearch implements SearchBehavior {
    /**
     * I could not make this class work in time, so the program only functions with the linear search. This class would be used to sort through the list with a binary search.
     * @param data
     * @param item
     * @return
     */
    public static boolean contains(ArrayList<String> data, String item) {
        return true;
    }
    
}
